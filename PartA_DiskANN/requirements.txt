# Runtime dependencies for the DiskANN-style prototype.
# Versions on the left are the ones the prototype was developed and tested
# against (Python 3.12.7). The >= bounds are conservative; the code uses
# only mainstream numpy / sklearn APIs and should work with newer versions.

numpy>=2.0          # 2.1.2 used in development
scikit-learn>=1.5   # 1.8.0 used in development; PQ training uses KMeans

# Optional. Only used by run_experiments.py; degrade gracefully if absent.
psutil>=5.9         # 7.2.2 used in development; for peak RSS reporting
pynndescent>=0.5    # 0.6.0 used in development; in-memory ANN baseline

# Optional tooling. Not needed to run the prototype or experiments.
matplotlib>=3.8     # make_figures.py only
pytest>=8           # test_sanity.py only
